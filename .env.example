# PDF转Markdown工具的环境变量配置
# 复制此文件为.env并根据需要修改

# 输入目录 - 存放PDF文件的目录
INPUT_FOLDER = ./source

# 输出目录 - 存放生成的Markdown文件的目录
OUTPUT_FOLDER = ./md

# 临时PDF目录 - 存放从Office文件转换的PDF文件(可选)
TMP_PDF_FOLDER = ./tmp_pdf

# 哈希值数据库文件路径 - 用于跳过未更改的文件(可选)
HASH_DB_PATH = ./md/file_hashes.json

# 注意：图片目录默认为OUTPUT_FOLDER/images，无需单独配置